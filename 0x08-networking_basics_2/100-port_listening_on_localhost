#!/bin/bash

# Listen on localhost (127.0.0.1) port 98 and output any received text
echo "Listening on port 98, waiting for connections..."

# Use nc (netcat) to listen on port 98 on localhost, and output received data
nc -l 127.0.0.1 98
